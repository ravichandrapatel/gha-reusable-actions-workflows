# Copy to repo-root `.secrets` (gitignored). KEY=value, one per line.
# Names only in the example — never commit real values.
#
# Common:
# GITHUB_TOKEN=ghp_...
#
# Optional GitHub App (non-release local workflows):
# APP_ID=
# APP_PRIVATE_KEY=
