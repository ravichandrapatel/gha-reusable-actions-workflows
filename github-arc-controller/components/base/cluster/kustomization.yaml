apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Truly cluster-scoped OpenShift/K8s objects (safe with overlay namespace:).
labels:
  - pairs:
      app.kubernetes.io/managed-by: argocd
      app.kubernetes.io/part-of: github-arc

resources:
  - scc-github-arc.yaml
  - clusterrole-scc.yaml
