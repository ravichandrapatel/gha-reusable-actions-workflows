metadata:
  id: CKV2_SPVS_1
  name: "SPVS: Each job must declare an explicit permissions block (least privilege)"
  category: IAM
  guideline: "Declare permissions at job level; do not rely on implicit GITHUB_TOKEN defaults."
definition:
  cond_type: attribute
  resource_types:
    - jobs
  attribute: permissions
  operator: exists
